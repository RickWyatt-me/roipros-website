---
interface Props {
  /** Optional width so header spacing stays consistent */
  width?: number;
}

const { width = 140 } = Astro.props;
---

<img
  src="/ROIpros%20LOGO_ONLY_COLOR.png"
  alt="ROIpros"
  width={width}
  class="h-auto select-none"
/>
